apiVersion: v1
kind: Service
metadata:
  name: employee-service
  labels:
    app: employee-service
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
  selector:
    app: employee-service
  type: ClusterIP